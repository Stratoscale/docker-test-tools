[pytest]
console_output_style = progress
filterwarnings =
    ignore::DeprecationWarning
    ignore:.*should be a three-digit integer followed*:Warning
    ignore:.*Monkey-patching*:Warning
addopts =
    --junitxml=reports/unittest.xml
